[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "theochem_live"
version = "0.1.0"
dependencies = [
    "numpy>=1.24",
    "requests>=2.31",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["src_live*"]

